services:
  mizuki:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mizuki
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 4321
    ports:
      - "127.0.0.1:4321:4321"
